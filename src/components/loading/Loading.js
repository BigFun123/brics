import { Typography } from "@mui/material";

export function Loading(props) {
    return (
        <Typography variant="h1" sx={{ textAlign: 'center', marginTop: '50vh' }}>
            Loading...
        </Typography>
    )
}